/** Canonical player-facing masthead copy stays separate from app orchestration. */
export const GAME_COPY = Object.freeze({
  mastheadEyebrow: "Fictional cancer-growth simulation",
  mastheadSubtitle: "One transformed cell. No exit interview.",
} as const);
